// import fonr chu tren goole -> then, dinh ngia hang mang gia tri latin
import { Coiny, Inter, Lusitana } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] });

export const lusitana = Lusitana({
  weight: ["400", "700"],
  subsets: ["latin"], // latin la font so cap
});
